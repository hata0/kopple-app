export type Person = {
  profiles: ProfileContent[];
}

export type ProfileContent = {
  age: number;
  hashtag: string;
  imageUrl: string;
  message: string;
  name: string;
  sex: string;
  birthday: string;
  last: string;
  address: string;
};